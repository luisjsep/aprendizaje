

def recibeLista(lista):
    #lista con largos
    largos=[]
    l_lista=len(lista)
    #largo de las palabras dentro de una lista en el mismo orden
    for i in range (len(lista)):
        largos.append(len(lista[i]))
    
 
            


listado=["aflmaxm","aflmin", "afldos"]
recibeLista(listado)


